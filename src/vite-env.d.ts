/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Время сборки, подставляется vite. Видно в настройках — по нему сверяют версию. */
declare const __BUILD_TIME__: string;
